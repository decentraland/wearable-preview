# Unity: use uncompressed local build assets in non-production.
VITE_RENDERER_USE_UNCOMPRESSED="false"

# Babylon: enable inspector/debug helpers.
VITE_REACT_APP_DEBUG="false"

# UI env bootstrap flag consumed through Vite define mapping.
VITE_REACT_APP_DCL_DEFAULT_ENV="true"
